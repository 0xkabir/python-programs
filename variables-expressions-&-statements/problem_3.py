# Write a program that calculates the area of a rectangle using length and width values entered by the user.

length = int(input("Enter the length (in meters): "))
width = int(input("Enter the width (in meters): "))

area = length*width

print(f"The area is {area} square-meters")